# Lenormand Prism 目录包

本目录版替代旧的 `LN_MATERIALS_SUPPLEMENT.zip`，供 Prism 直接读取。所有嵌套 ZIP 已递归解包，包内不包含 ZIP 容器；单文件上限为 10 MB。

- 只读设计、资料覆盖、历史回包和审计证据。
- 不代表资料批准、运行授权、代码完成或发布资格。
- 目录中的候选、Astra、Book T、Mentor 和 LN 资料必须按生命周期重新核验。
- 原始相对路径保留在 `payload/` 下；递归解包关系记录在 `DIRECTORY_EXTRACTION_INDEX.json`。
