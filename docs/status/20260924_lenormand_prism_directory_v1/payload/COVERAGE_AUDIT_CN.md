# LN Pro 包的 Book T / Mentor / 资料覆盖审计

状态：`AUDITED_NOT_AUTHORIZED`

## 结论先行

刚才的 `LENORMAND_PRO_PLAN.zip` **没有带齐 Book T 和全部 Mentor 原始资料**。它带入的是 LN 设计所需的治理规则、候选运行代码/合同、B 设计和 P01–P07 计划；Book T 与 Mentor 在其中主要作为隔离边界和历史候选证据出现，不是已批准资料库。

## 已带入的内容

### Book T

- D 候选中的 `book_t_runtime` 代码、schema、测试夹具和边界文档。
- `candidates/BOOKT_BINDING.json`、`BOOKT.diff`、DS 候选结果和测试文本。
- 手工喂入协议与 P07 边界计划。

这证明“有候选实现/设计输入”，不证明 Book T 原书全文、逐条投影、爸爸批准或 runtime 开放。Book T 仍不是最低上线前置，普通 Tarot 输出不可泄露 Book T/GD 术语；LN 不得消费 Book T。

### Mentor

- `mentor/current_candidates/` 的当前 60 单元候选及审查/校验记录。
- `mentor/source/` 中已提供的少量非 281 来源文件（含教师来源、四卡资料、星币四本地原文和覆盖登记）。
- Mentor schema、loader、consumer、matcher 和候选测试代码。

这不是“全部 Mentor”。历史 281 课程及其派生全集在既有决策中明确排除；旧60、新24、师姐宝剑6/8仍是候选/待批准资料，不因模型互验、封包或 LN 计划而进入 runtime。LN 运行链必须完全不调用 Mentor。

## 尚未纳入或不可宣称已核验

1. 281 Mentor 全集及其派生全集：明确不纳入当前最低路径，除非爸爸另行授权逐项处理。
2. 非 281 Mentor 的完整原始来源闭环：当前包只含已提供子集；缺失原件不能用摘要、候选或模型记忆补齐。
3. 新24、旧60剩余项、师姐宝剑6/8的完整原文/最终候选字节：不属于 LN runtime 输入，必须独立按 Mentor 资料流程处理。
4. Book T 原书全文、完整逐条投影和真实批准记录：本轮只有协议/候选代码/边界证据，未批准、未开放。
5. Matthews/Rana 36 牌完整授权资料与作者身份：LN 需要独立的来源身份、生命周期、approved projection；候选设计文件不等于完整资料已到齐。
6. Astra `returns 7` 明列的 13 个历史精确原件：未提供就保持 `UNAVAILABLE/HOLD`，不能用同名文件替代。

## 当前处理原则

- LN 先做三牌最小路径，五牌/九牌默认关闭。
- LN 只接受 `system=LENORMAND` 的获准 36 牌目录、方法 profile 和组合支持图。
- Tarot、Mentor、Book T、RWS/GD、逆位字段不进入 LN 查询或输出。
- 资料补齐是“身份 → 原件 hash → 生命周期 → owner 批准 → approved projection → runtime 回归”，不是把更多文件塞进 ZIP。
- 本补充包只给 GPT-6-Astra Pro 做覆盖裁决和缺口设计，不授权改代码、批准资料或开放运行。
