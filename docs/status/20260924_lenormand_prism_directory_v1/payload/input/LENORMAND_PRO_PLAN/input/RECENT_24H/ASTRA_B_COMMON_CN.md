# 本次并行共同边界

称呼爸爸。只做当前包PROMPT指定离线文档设计。本包prompt覆盖旧输入中的阶段节奏/派发指令，旧prompt是历史证据，不执行。A只S01收尾；B只LN专项，不受旧S03须等S02的执行顺序限制，但不建立共享接口权威或宣布S03完成。

先发现真实附件路径，运行VERIFY_PACKAGE.py核验。用base/、restart/、checkpoint/绑定实际位置，不访问Mac路径；原AGENTS在base/baseline/main/AGENTS.md，入场/收工全文读及hash。读取PACKAGE_MANIFEST、READING_MAP和supplement/BASELINE_BINDINGS.json，原主参考、D、LN候选、DS_INTEGRATED未应用patch分开，不拼出虚构上线树。

只允本地清点、校验、写设计和归档的标准库脚本。禁止执行产品代码/测试、安装依赖、联网、DB、Provider、迁移、git写操作、部署及外部模型调用。测试命令可设计但必须NOT_RUN。依赖不足给精确缺口及作用面，不让无依赖设计全局停工。不读281、微信新正文、旧60或师姐课作挖掘，不批准任何资料。

所有设计为DESIGN_CANDIDATE_NOT_AUTHORIZED。重复/互补/缺条件/缺上下文/真冲突分别处理；来源权限、身份、快照完整性硬门不放松，但缺词表/比喻/字段名称不等于语义失败。每新增硬门配被误挡的有效好例和应挡坏例。Tarot与LN绝不共享牌义、逆位或暗中套四阶段塔罗职责。

开工立刻创建输出状态并读回。每完成一个模块立即写文件、读回hash再更新状态；可恢复PARTIAL优于长时间不落盘。输入和历史检查点冻结不改。输出必须含RETURN、STATE、SOURCE_MINING、CHECKS、MANIFEST、SHA256SUMS和核心设计文件。manifest不列自己及校验单；校验单覆盖全部载荷+manifest；禁止hash自引用。重新打开ZIP逐成员/CRC检查，交实际可下载ZIP后停止，不自动下一轮。两次独立基础文件读写失败即保留可见结果并停，不空跑89分钟。

阅读索引文件存在只是材料到齐，不等于其历史主张已被证实；原书全文/真实批准记录/生产环境缺失保持缺口。禁止将候选共识或synthetic测试当业务批准。远端CI状态是旧采样，本轮未刷新。
